{
  "permissions": {
    "allow": [
      "Bash(pnpm build)",
      "Bash(pnpm dev)",
      "Bash(pnpm install)",
      "Bash(pnpm add *)",
      "Bash(git *)",
      "Bash(gh *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(echo $CLAUDE_TOOL_INPUT | jq -r '.file_path // .filePath // empty'); if [[ \"$file_path\" == *.ts || \"$file_path\" == *.tsx ]]; then echo '[Auto] TypeScript file modified - run pnpm build to rebuild'; fi"
          }
        ]
      }
    ]
  }
}
